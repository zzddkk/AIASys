# npm 配置：让安装结果不受外部 NODE_ENV 影响。
#
# 背景（2026-08-09 实测）：本机 NODE_ENV=production 是环境变量层面设置的，
# npm 会读它并自动推导出 omit=dev，于是 `npm ci` / `npm install` 静默跳过
# 全部 17 个 devDependencies（typescript / eslint / @types/* 等）。
#
# 症状是一连串看起来毫不相干的故障：
#   - `tsc` / `eslint` 找不到（node_modules/.bin 里没有）
#   - `tsc -b --noEmit` 报 4 条 TS2307（prismjs / @codemirror/language / @lezer/highlight）
#     —— 这些其实是 @codemirror/lang-* 的间接依赖，包在 lock 里，只是没被装上
#   - lefthook 的 frontend-type-check 永久失败，开发者只能 `git commit --no-verify`
#   - `npm run build` 挂在 `tsc -b` 这一步
# 当时误判为「代码里有预存的类型错误」，实际是装依赖时被 NODE_ENV 截掉了一半。
# CI 不受影响，因为 GitHub Actions 里 NODE_ENV 不是 production——于是「CI 绿、本地坏」。
#
# include=dev 显式要求安装 dev 依赖，优先级高于 NODE_ENV 推导出的 omit。
include=dev
